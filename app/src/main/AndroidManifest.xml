<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolist">

    <application
        android:name=".ToDoList"
        android:allowBackup="true"
        android:icon="@mipmap/todo_note"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/todo_note"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".DeleteDialogActivity" />
        <activity
            android:name=".FinishToDoActivity"
            android:theme="@style/CustomAlertDialog" />
        <activity
            android:name=".AddToDoActivity"
            android:theme="@style/CustomAlertDialog" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>